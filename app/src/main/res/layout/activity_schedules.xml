<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/darker_orange"
    tools:context=".activity.SchedulesActivity">

    <TextView
        android:id="@+id/text_view_main_schedule_activity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:padding="4dp"
        android:text="@string/list_containing_the_schedule_for_s_buses"
        android:textAlignment="center"
        android:textColor="@color/white_smoke"
        android:textSize="32sp"
        android:theme="@style/FontGlobalRegular" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:padding="4dp"
        android:text="Tap on a schedule to check from the other direction and tap on it longer to see the route on the map."
        android:textAlignment="center"
        android:textColor="@color/white_smoke"
        android:textSize="16sp"
        android:theme="@style/FontGlobalLight" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/schedules_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</LinearLayout>